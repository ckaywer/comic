import React, { Component } from 'react';
import { Platform, Image, StatusBar, StyleSheet, View, TouchableOpacity, Easing, Animated } from 'react-native';
import { Text } from 'react-native-elements';
import { Images } from '../../constants/images';

export default class Home_Comic extends React.Component{

  render() {

    return (
        <View>
                <Text>home_comic</Text>


        </View>
    );
  }

}
